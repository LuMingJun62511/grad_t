# 蒸馏数据清洗全流程

## 背景

- **数据来源**：DeepSeek V4 蒸馏生成，6000 条，含思维链（CoT）+ 情感极性标签
- **训练基座**：Qwen 3.5（8-32B）
- **任务**：VOC 社媒场景情感极性判断
- **核心问题**：6000 条数据量太大，无法逐条人工审核；蒸馏数据可能存在错标、CoT 胡说、重复等问题
- **核心策略**：用自己的基座模型做 4 折 CV 筛选可疑样本，有的放矢地审阅

---

## 全流程总览

```
第 0 步（嵌入去重）→ 第 0.5 步（50条摸底）→ 第 1 步（4折CV筛选）
→ 第 2 步（置信度分层）→ 第 3 步（跨源交叉校验）
→ 第 4 步（人工复审）→ 第 5 步（抽样验证标对数据）
```

---

## 第〇步：嵌入去重

**耗时**：约 10 分钟（代码跑）

**方法**：
1. 用 Qwen 3.5 最后一层 hidden state 对每条数据做句子嵌入
2. 计算 pairwise cosine similarity
3. 设定阈值（建议 0.92-0.95），高于阈值的视为重复
4. 每组重复保留一条

**目的**：蒸馏数据中"看起来不一样但实际一样"的样本先砍掉，减少后续工作量。

---

## 第〇点五步：50 条随机摸底

**耗时**：约 20 分钟

**方法**：
1. 从去重后的数据中随机抽 50 条
2. **关掉 CoT**，让 Qwen 3.5 基座直接预测情感标签
3. 对比 Qwen 3.5 预测结果与 DSv4 标签
4. 计算**原生分歧率** = 不一致的条数 / 50

**判断标准**：

| 分歧率 | 结论 | 后续动作 |
|--------|------|----------|
| < 5% | 数据质量靠谱 | 放心往下走 |
| 5%-15% | 有一定风格差异 | 关注后续筛选结果 |
| > 15% | DSv4 标注风格与 Qwen 理解方式不兼容 | SFT 前考虑标签风格对齐 |

**目的**：花最少的时间拿到一个数据质量的底线判断，避免后续所有工作建立在错误假设上。

---

## 第一步：4 折 CV 筛选可疑样本

**耗时**：代码训练 4 轮 + 推理（取决于硬件，预计几小时），人力几乎为零

**方法**：
1. 将数据随机分成 4 折
2. 每轮用 3 折训练 Qwen 3.5、1 折做预测
3. 收集所有 4 轮中**预测标签 ≠ DSv4 标签**的样本
4. 这些就是"可疑样本"

**关键原则**：
- **CV 只看最终标签对不对，不看 CoT 质量**
- CoT 部分暂时不管——DSv4 的推理深度 Qwen 8B 大概率学不会

**预期产出**：
- 筛出约 800-1500 条可疑样本
- 审阅量从 6000 条砍到 800-1500 条（减少 75%+）
- 预测正确的样本（约 4500-5000 条）暂时标记为"干净数据"

---

## 第二步：可疑样本置信度分层

**耗时**：代码自动完成

**方法**：
将第一步筛出的可疑样本按模型预测置信度分两层：

| 分层 | 特征 | 大概率是 | 优先级 |
|------|------|----------|--------|
| **高置信度错误** | 模型很确信但预测错了 | 真错标 | 🔴 优先看 |
| **低置信度错误** | 模型摇摆不定，概率分布均匀 | 难例 | 🟡 保留但标记 |

**获取置信度的方式**：
- 优先：模型输出 logits / probability
- 备选：多次推理（temperature=0.7，跑 3-5 次），看答案一致性 → 不一致即为低置信度

---

## 第三步：跨源交叉校验

**耗时**：代码自动完成（推理）

**方法**：
1. 用一个**不同体系的小模型**（如 ChatGLM 3-6B、DeepSeek 小版本、或任何非 Qwen 系的模型）对可疑样本做推理
2. 对比三个来源：DSv4 标签 vs Qwen 3.5 预测 vs 第三模型预测

**判断逻辑**：

| 局面 | 判定 | 处理 |
|------|------|------|
| 三方一致（标签 = Qwen 预测 = 第三模型预测） | 标签大概率正确，Qwen 只是不稳 | 保留，标记为难例 |
| 标签对，但 Qwen 和第三模型都错了 | 标签对但模型学不会（能力差距） | 保留数据，别强求学 CoT |
| 标签错，Qwen 和第三模型一致 | 标签确实有问题 | **修正标签**，进入复审 |
| 三者各不同 | 样本本身有歧义 | 人工判断，可能需要细化标注规范 |

**目的**：用最低成本消除"DSv4 和 Qwen 3.5 一起错"的系统性盲区。

---

## 第四步：人工复审

**耗时**：视可疑样本量而定，每条几秒

**方法**：
1. 只审高置信度错误样本（第二步标记为 🔴 的）
2. **只看最终标签，不看 CoT**
3. 对每条做快速判断，分两类处理：

| 判定 | 特征 | 处理方式 |
|------|------|----------|
| **标签确实错了** | DSv4 在反讽、行业黑话、歧义短文本上失误 | 修正标签；CoT 可保留或丢弃 |
| **标签对但模型学不会** | Qwen 8B 够不到 DSv4 的推理深度 | 保留数据；SFT 时用简化版 prompting，不要求复现 CoT |

**重点关注的样本类型**（DSv4 最容易在这几类上出错）：
- 反讽 / 阴阳怪气（"这售后速度，我奶奶都比我快"）
- 领域特有表达（行业黑话、产品昵称）
- 多义短文本（"还行吧"——中性还是偏正面？）

---

## 第五步：抽样验证"标对的数据"

**耗时**：约 1-2 小时

**方法**：
1. 对 CV 预测正确的 4500-5000 条样本，按置信度分层
2. 随机抽取 100-200 条
3. 人工检查标签质量

**判断标准**：
- 抽查通过率 > 95% → 剩下的干净数据直接用于训练
- 抽查通过率 < 95% → 扩大抽查范围，或考虑第二轮 CV 筛选

**目的**：验证"CV 预测正确 = 标签干净"这个假设是否成立，避免漏掉系统性错误。

---

## 核心区分：一张决策表

| 样本类型 | 来源 | 怎么处理 |
|----------|------|----------|
| **真错标** | DSv4 失误 | 修正标签 |
| **难例**（标签对，模型不稳） | 业务边缘 case | **保留**，是高价值训练数据 |
| **超纲例**（标签对，模型学不会） | Teacher-student 能力差距 | **保留**，降低 CoT 要求 |
| **简单例**（标签对，模型也稳） | 正常数据 | 直接使用 |
| **重复例** | 蒸馏产生的内容重复 | 去重删除 |

---

## 成本汇总

| 步骤 | 人力耗时 | 计算耗时 |
|------|----------|----------|
| 嵌入去重 | 0 | ~10 分钟 |
| 50 条摸底 | ~20 分钟 | 少于一分钟 |
| 4 折 CV | 0 | 几小时（取决于硬件） |
| 置信度分层 | 0 | 自动完成 |
| 跨源校验 | 0（如有可用第三方模型） | 推理时间 |
| 人工复审 | 1-3 小时（看筛出量） | N/A |
| 抽样验证 | 1-2 小时 | N/A |
| **合计** | **约 3-6 小时** | **几小时** |

---

## 这件事的本质

你不是在做数据清洗。你是在做 **"DSv4 标签风格 → Qwen 3.5 学习能力"的适配映射**。

在这个过程中产生的样本分类（错标、难例、简单例、超纲例、风格不兼容例），本身就是一份有价值的交付物——它可以告诉领导和团队成员：**蒸馏数据的质量画像是什么样的，哪些类型的数据需要重点关注，哪些可以放心批量使用。**

这份认知可以外推到后续其他任务（意图识别、实体抽取等）的数据清洗流程中。
