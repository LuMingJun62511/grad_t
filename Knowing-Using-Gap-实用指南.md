# 从"记住了但用不上"到可操作的训练改进

> 基于论文 *Towards Mechanistically Understanding Why Memorized Knowledge Fails to Generalize in Large Language Model Finetuning*（Lu Dai et al., 2026）的实用提炼。
>
> **核心发现：** 微调注入新知识时，模型背诵事实的速度远快于学会组合使用事实的速度。背诵完成后 loss 归零、梯度消失，知识表征无法自然渗透到推理计算层——这就是 Knowing–Using Gap。

---

## 一、可以直接用的（零额外成本）

### 1.1 训练时同时跟踪两条曲线：记忆准确率 vs 泛化准确率

这是整篇论文里对你最有用的一条操作建议。

**做法：**

在微调过程中，除了看 loss 和下游评测，额外维护两个评估集：

| 评估集 | 题型 | 考察什么 | 例子 |
|--------|------|---------|------|
| 记忆评估集 | 单跳事实问答 | 模型"背下来没有" | "悉尼在哪个国家？" → 澳大利亚 |
| 泛化评估集 | 多跳组合问答 | 模型"能不能用" | "悉尼所在国家的首都是什么？" → 堪培拉 |

两个评估集的答案所需的**底层知识完全相同**，差别仅在是否需要组合推理。

**在每个 epoch 或每 N 步后同时跑这两个评估集，画两条曲线。**

**读数指南：**

```
两条曲线紧密同步上涨
  → 健康。知识和推理能力在同时增长。

两条曲线分叉，记忆线逼近天花板，泛化线低位停滞
  → 你遇到了 Knowing–Using Gap。
  → 继续训下去大概率不会好转（梯度已消失）。
  → 需要干预，不要只加 epoch。

泛化线先涨后跌
  → 可能出现了灾难性遗忘或过拟合。
  → 检查评估集的多样性、训练数据的配比。
```

**为什么这比看 loss 好：**

loss 到零只能告诉你"模型记住了训练数据"，不能告诉你"模型学会了使用这些知识"。两条曲线的分叉点 = loss 给不了的诊断信号。

### 1.2 训练数据中必须混入组合推理题

论文的机制分析揭示了一个关键动态：

> 记忆题的 loss 先归零 → 梯度消失 → 模型停止内部调整 → 知识表征"搁浅"在存储层，无法继续向计算层渗透。

**这意味着：** 如果你的训练数据全是单跳事实问答对，模型会很快收敛，然后**在内部表征还没建立好路由之前**就停止学习了。

**对应策略：**

- 在微调数据中保持一定比例的**多跳推理题**（两跳或更多），不要只喂单跳事实。
- 多跳推理题的 loss 比单跳记忆题更难归零，它们**持续产生梯度**，推动知识表征向中层计算层扩散。
- 并不需要多跳题占比很高——论文的发现是，哪怕少量的组合推理训练样本，其产生的梯度信号也足以在记忆 loss 归零后继续维持表征的内部优化。

**具体配比：**

论文没有做系统性消融，但基于其机制可以推导的保守建议：

| 训练阶段 | 单跳记忆题 | 多跳推理题 | 理由 |
|---------|-----------|-----------|------|
| 早期（前 30% 训练） | 70–80% | 20–30% | 先让知识编码进模型 |
| 中期 | 50–60% | 40–50% | 逐步加大路由训练的比重 |
| 后期 | 30–40% | 60–70% | 记忆已饱和，主攻路由 |

如果无法划分阶段，一个简单的规则是：**多跳题永远不要低于 30%。**

### 1.3 同时监控"训练完成了"不等于"可以上线了"

论文发现即使记忆准确率 99%+ 且 loss 接近零，泛化准确率也可能只有 12–18%。

**工程启示：**

- 不要把记忆评估集的准确率作为模型上线的唯一判断标准。
- 上线前必须跑泛化评估集（多跳推理、组合使用场景）。
- 如果记忆准确率 > 95% 但泛化准确率 < 50%，这个模型**没有真正学会**——延长训练不会解决这个问题，要回去改训练数据配比。

---

## 二、值得探索的（需要投入实验验证）

### 2.1 中间层用不同的学习率

论文 Figure 5 揭示了一个跨模型、跨领域一致的规律：

```
知识存储位置：
  浅层（前 ~10% 层）  → 实体识别、上下文语义编码
  深层（后 ~20% 层）  → 经过全深度处理的富集语义

计算瓶颈位置：
  中间层（~45–55% 层）→ 桥接推理、属性提取、事实组合
```

**启发：** 如果标准微调对所有层使用相同学习率，而知识编码主要发生在浅层和深层（梯度强），中层（计算层）梯度相对弱——那中层就更新不足，形成"知识在两边但中间缺连接"的局面。

**可实验的方向：**

1. **分层学习率衰减**：对中间层（40–60% 深度范围）使用更高的学习率，浅层和深层使用标准甚至更低的学习率。
2. **选择性全量微调**：如果使用 LoRA，可以考虑对中间层的 attention 或 MLP 模块使用更高的 rank，甚至对中间几层做全量微调而其余层保持 LoRA。
3. **渐进解冻**：先冻结中间层只训练浅层和深层（编码知识），然后解冻中间层继续训练（建立路由）。

**注意：** 这些都是基于论文发现的推论，论文本身没有做这些实验。需要你在自己的场景中验证。

### 2.2 训练后期插入"路由锚点"任务

论文的一个关键发现是：梯度在记忆 loss 归零后消失，导致表征无法继续向计算层扩散。而成功的泛化 case 恰好是**在记忆饱和后仍然有梯度信号持续推动表征扩散**的那些。

**实操思路：**

在训练的后期阶段（记忆准确率已达到 90%+ 时），植入一批高难度的多跳推理题——不是为了考察模型，而是**纯粹为了创造梯度**：

- 这批题目的知识基础与已训练的事实完全重叠
- 但推理链更长（三跳而非两跳）
- 或者引入同义改写、否定句式等变体

这些题目的 loss 不会轻易归零，它们产生的持续梯度可能恰好驱动表征完成从存储层到计算层的最后一段渗透。

**判断标准：** 如果加入这批题后，泛化评估集的准确率在 5–10 个 epoch 内明显提升了（而记忆评估集已经不变），说明路由在建立——策略有效。

### 2.3 考虑从数据层面显式标注"桥接实体"

论文的 Chaining 任务结构是 > 实体 A → 关系1 → 桥接实体 B → 关系2 → 答案 C。

问题出在模型需要从"实体 A + 关系1"推导出桥接实体 B，再从"桥接实体 B + 关系2"推导出答案 C。如果模型在第一步就失败了（没有正确检索到桥接实体），第二步自然无法完成。

**数据层面的实验方向：**

在训练数据中显式包含"桥接事实"作为中间监督信号。例如不只在数据中放：

```
Q: 被ABCC11运输的药物的副作用是什么？
A: Vertigo
```

而是同时放：

```
Q: 什么药物被ABCC11运输？
A: Probenecid

Q: Probenecid的副作用是什么？
A: Vertigo

Q: 被ABCC11运输的药物的副作用是什么？（需综合以上两个事实）
A: Vertigo
```

**为什么可能有效：** 这给了模型额外的梯度信号来强化桥接实体（Probenecid）的表征，使其更容易在深层被检索到。论文的机制分析暗示桥接实体的表征质量是推理链路的关键瓶颈。

---

## 三、不要做的（基于论文的负面发现）

### 3.1 不要指望"多训练几个 epoch 就好了"

论文发现有些 case 即使训练到 50 个 epoch（远在记忆饱和之后），泛化准确率依然是 0。这不是训练不足的问题——是梯度消失后模型内部不再发生变化。

**如果你观察到记忆和泛化曲线的分叉持续超过 5–10 个 epoch 且差距没有缩小趋势——停下来，改策略，不要加 epoch。**

### 3.2 不要指望"更大的模型就能解决"

Figure 2 和 Table 10 的数据：

| 模型规模 | Chaining ΔT |
|---------|------------|
| Qwen-2.5-1.5B | 4.6 epochs |
| LLaMA-3.2-8B | 4.6 epochs |

从 1.5B 到 8B，时间滞后几乎不变。而且**训练数据量越大，最终的准确率差距 ΔA 反而越大**——更多的知识意味着更多的路由失败。

**这意味着：** Knowing–Using Gap 是结构性的训练动力学问题，不是容量问题。堆算力不能绕过它。

### 3.3 不要试图在推理时做表征搬运

论文的 Fixed Heuristic（固定两个层对在推理时做跨层表征搬运）是学术证明的一部分——它的存在是为了说"你看，就算不 per-sample 搜索也能恢复不少"，**不是建议你在生产环境中这么做**。

实际部署中的问题：
- 需要跑多次前向传播（提取 + 注入 + 重算），延迟翻倍甚至更多
- 不同任务、不同输入格式可能需要不同的层对配置
- 和现有的推理优化（KV cache、speculative decoding、batching）几乎不可能兼容
- 打包、部署、维护成本远超过收益

论文这部分的价值**只在于证明结论的稳健性**，不在于提供工程方案。

### 3.4 不要把论文的核心假说当成既定事实

"知识回路错位假说"（knowledge-circuit misalignment）是一个有价值的工作假说，但论文的证据是**因果干预**（搬过去有效），不是**直接观测**（看到路由没建立）。搬运可能混杂了上下文驱动的局部计算结果，纯路由解释并未被严格证明。

**让你知道这个不是为了让你怀疑论文，而是为了让你在自己的实验中保持清醒——如果你试了上面的方法但效果不好，不要被困在"一定是路由问题"的思维框里，可能还有别的原因。**

---

## 四、一张操作速查表

| 你观察到的现象 | 诊断 | 优先尝试 | 不要做 |
|-------------|------|---------|--------|
| loss 到零了但下游推理很差 | 典型的 Knowing–Using Gap | 检查数据配比，加入多跳推理题 | 不要继续加 epoch |
| 记忆和泛化曲线分叉后不再收敛 | 梯度消失，路由训练停止 | 训练后期插入高难度推理题作为梯度锚点 | 不要只扩大模型 |
| 训练数据量增大但泛化反而变差 | 更多知识 = 更多路由失败 | 保持多跳题比例不低于 30%，考虑分层学习率 | 不要减数据，调整配比即可 |
| 模型上线后"会背不会用" | 训练评估只看了记忆指标 | 引入泛化评估集作为上线门禁 | 不要只看 loss 或单跳准确率 |
| LoRA 效果不够好 | 低秩可能限制了中层计算更新 | 尝试对中间层提高 rank 或做选择性全量微调 | 不要不验证就改成全量微调 |

---

## 五、核心思想转变

这篇论文对你最有用的，不是它的结论，而是它帮你完成的一个**思维转变**：

| 以前的直觉 | 现在的精确认知 |
|-----------|-------------|
| "模型学会了但用不上" | "记忆准确率 97%，泛化准确率 22%，ΔT ≈ 4 epochs" |
| "再训练一会儿看看" | "分叉超过10个epoch没收敛，梯度已消失，改策略" |
| "数据越多越好" | "知识量增大会让路由失败率增加，需要配套增加多跳题" |
| "loss 下来了就行了" | "loss 归零 = 记忆完成 ≠ 训练完成" |
| "模型可能没学会" | "可能学会了但知识在错误的位置——干预训练策略而非数据规模" |

你不需要做表征搬运，不需要理解 self-patching 的细节——你只需要**在训练时多放一条泛化评估曲线、多混一些多跳推理题、在看到分叉时及时干预而不是盲目加 epoch**。这三件事加起来，就是这篇论文对一线训练工作的全部实用价值。
