# 贴文改写：schema 分级与提示词（照做版 v1）

- 日期：2026-09-19
- 用途：构造定向评测集的改写环节——把贴文中的产品指称改写成"贴近知识注入"的形式（别名/特征/参数指代），让 base 大跌、+KI 大涨。
- 配套：《华为耳机-三元组试验田.md》《SFT知识注入-收官冲刺计划.md》

## 一、栏位分级（schema 先行，只让高级别进改写）

| 级 | 栏位 | 例子 | 用法 |
|---|---|---|---|
| **S 直接可用** | 形态/佩戴方式 | "华为那个夹耳朵的" | 主料 |
| | 品类+形态 | "华为耳夹耳机" | 主料 |
| | 系列名/型号/别名 | "FreeClip""fbp5""悦彰" | 主料 |
| | 关键卖点 | "能点头接电话的那个""星闪耳机" | 主料 |
| | 价格段 | "一千多的""百元档" | 辅料 |
| | 代际 | "华为新款""去年那款" | 辅料 |
| **A 转译后可用** | 数值 → 体验语 | 5.1g → "很轻"；38h → "续航能打"；52dB → "降噪顶级" | 走第二节转译表 |
| | 区间近似 | "续航快 40 小时" | 评审文档特坑 1 的结论 |
| **B 不用** | 精确重量、电池 mAh、蓝牙版本号、频响、充电盒重量 | "单耳 5.1g""510mAh" | 永不出现在改写里 |

两条规则：

1. **数值必须转译成体验语再进改写**。精确参数是怪句子的主要来源。
2. **唯一性要求放松**。"华为耳夹耳机"命中 2 款也没关系——大跌大涨只需要模型认出"这是华为耳机相关贴文"，不需要唯一指认到 SKU。组合命中 2~N 可用。

## 二、数值转译表（A 级栏位用）

| 原值类型 | 转译成 | 例子 |
|---|---|---|
| 重量 g | 轻/重 | 5.1g → "很轻"；6.1g → "有点分量" |
| 续航 h | 续航能打/一般 | 38h → "续航能打"；28h → "续航一般" |
| 降噪 dB | 降噪顶级/还行 | 52dB → "降噪顶级"；25dB → "有降噪" |
| 电池 mAh | 不用 | —— |
| 蓝牙版本号 | 不用 | —— |

转译原则：体验语不带数字；必须带数字时用区间近似（"快 40 小时"），不用精确值。

## 三、四指标仪表盘（迭代的仪表）

| 指标 | 怎么算 | 门槛 |
|---|---|---|
| ① 贴近原句 | 编辑距离 ÷ 原句字符数 | ≤ 40%（超过 = 重写不是改写） |
| ② 不怪异 | LLM 裁判 1–5 分，rubric 带锚例（1 = "单耳5.1g、C形夹那款"，5 = 看不出痕迹） | ≥ 4 |
| ③ 指代可解 | 改写句回查：能否映射回目标产品（规则表 + 抽检） | 100% |
| ④ 标签保持 | 改写前后标签一致 | 100% |

③④ 是硬门槛，不达标直接弃用该条；①② 是质量分，低了改提示词。

## 四、改写提示词 v2（可直接跑）

```
你是一个贴文改写助手。任务：把给定贴文中"对产品的指称"替换成"用产品特征指代"的形式。

规则：
1. 只替换产品指称部分（产品名/型号/昵称），其余内容一字不改。
2. 替换只能使用"事实卡"里给出的信息，禁止新增任何事实卡外的信息。
3. 禁止改变原句的情绪、意图、话题类型（噪声贴文改写后必须仍是噪声贴文）。
4. 优先用事实卡里的"自然指称"（形态/别名/卖点），参数只在自然时用，禁止参数堆砌。
5. 输出 JSON：{"rewritten": "...", "replaced_span": "原文指称", "used_attrs": ["耳夹式"]}

事实卡：{"目标产品": "FreeClip 2", "自然指称": ["华为耳夹耳机", "夹耳的那款", "星闪耳机"],
         "卖点": ["点头接电话"], "价格段": "一千多", "代际": "新款"}

示例（对）：
原句："刚入了 FreeClip 2，通勤听歌很舒服。"
改写："刚入了华为那个夹耳朵的，通勤听歌很舒服。"

示例（错1，参数堆砌）："刚入了单耳5.1g、C形夹的那款耳机，通勤听歌很舒服。"
示例（错2，编造信息）："刚入了新耳机，戴着很轻很舒服。" ← "很轻"不在事实卡里

贴文：{post}
```

要点：

- 事实卡里**不放 B 级参数**，从源头堵住怪句子；
- "随机选属性"只随机在 S/A 级自然指称里选，不从全部栏位里选；
- 输出 `replaced_span` 是为了直接算指标①，输出 `used_attrs` 是为了校验指标③。

## 五、v3 两步法（v2 的指标②仍 <4 时启用）

- **第一步**：LLM 只输出"指称候选短语"（从事实卡选 2–3 个属性拼短语）→ 此时单独校验指标③（指代可解）。
- **第二步**：用规则把候选短语拼回原句，只做替换 → 指标①天然达标（改动只发生在替换处）。

两步分开检查，问题各归各：候选短语错 → 查事实卡；拼回生硬 → 查候选短语的自然度。

## 六、迭代循环（机械跑）

1. 取 20 条贴文（10 噪声 + 10 非噪声）跑 v2；
2. 四指标打分；
3. 哪个指标低改哪个变量：

| 指标低 | 改什么 |
|---|---|
| ① 贴近原句爆表 | 加强"一字不改"约束；加 1 个违规惩罚示例 |
| ② 不怪异低 | 换事实卡内容（多给 S 级、少给参数）；启用 v3 |
| ③ 指代不可解 | 查事实卡正确性；查转译表映射 |
| ④ 标签变了 | 加"不改情绪/意图"约束；重点审噪声贴文的改写 |

## 附：开工前的半天预实验

从社媒挖 20 条别名/特征指称，直接问 base 模型"这说的是哪款产品"。答不出的才进改写池——答得出的别名用了等于白用，大跌跌不下去。
