# 上下文感知的实体别名收束 — 训练方案

> 目标：让模型具备"从多个别名收束回产品本名"的能力，同时保持通用能力不退化
>
> 适用场景：8-9B 基座模型，开源基座 + CPT/SFT，GPU 资源充裕

---

## 目录

- [一、问题介绍](#一问题介绍)
- [二、应用场景与共性](#二应用场景与共性)
- [三、问题本质分析](#三问题本质分析)
- [四、方案选型](#四方案选型)
- [五、技术方案设计](#五技术方案设计)
- [六、执行计划](#六执行计划)
- [七、风险与应对](#七风险与应对)

---

## 一、问题介绍

### 1.1 什么是别名收束

在企业业务场景中，一个产品除了官方名称（本名），往往在用户口中存在多种变体——花名、别名、俗称、缩写、错别字变体。这些变体呈**发散状**从本名向外延伸，形成一棵**多叉别名树**：

```
                         ┌── "菊花厂"
                         │
                         ├── "华为公司"
                         │
                         ├── "华为技术"          ┌── "Mate 60"
                         │                      │
    华为 ←── 本名 ──→     ├── "Huawei"    ←── →  ├── "Mate60 Pro"
    (Huawei)             │                      │
                         ├── "花厂"              └── "遥遥领先"
                         │
                         ├── "那家做通信的"
                         │
                         └── "HW"
```

> **问题**：模型面对 "菊花厂" 时，能否理解这就是 "华为"？面对 "遥遥领先" 时，能否知道这是指华为 Mate 系列？

### 1.2 问题的两个方向

```
    方向一（发散）：本名 → 多个别名
        模型知道华为有很多叫法，这不难

    方向二（收束）：多个别名 → 本名  ← 这是我们的痛点
        用户说"菊花厂"、"花厂"、"HW"、"那家通信公司"……
        模型都要能归一化到"华为"
```

### 1.3 收束的边界：不该收束的时候不能收束

别名收束不是无脑映射。有一个经典的反例：

```
"刘德华为小明挤了5g牙膏"

┌─────────────────────────────────────────────────────┐
│  "华为" 在这里只是 "刘德华"+"为" 的字面巧合         │
│  "5g" 在这里是 "5克"，不是 "5G通信"                 │
│                                                     │
│  如果模型把这些也收束了，就会闹笑话                   │
│  → 过度敏感 = 另一种形式的失败                       │
└─────────────────────────────────────────────────────┘
```

**核心挑战**：模型需要判断，一个别名字符串在句子中是否作为**指称实体**（主语/宾语/名词性成分）出现，只有指称用法才需要收束。这就要求模型具备**上下文感知的实体识别能力**，而非关键词匹配式的规则。

### 1.4 问题的完整画像

```
┌────────────────────────────────────────────────────────────┐
│                      别名收束问题                            │
│                                                             │
│  输入：用户说 "最近菊花厂的遥遥领先还值得买吗"                │
│                                                             │
│  期望：模型理解 "菊花厂" = 华为， "遥遥领先" = Mate 系列      │
│        并在此基础上进行意图理解和回复                         │
│                                                             │
│  约束：用户说 "刘德华为小明挤了5g牙膏"                        │
│        模型不能触发任何收束，正常理解这句话                    │
│                                                             │
│  本质：这不是一个"关键词替换"问题，                           │
│        而是一个"实体指称理解"问题                             │
└────────────────────────────────────────────────────────────┘
```

---

## 二、应用场景与共性

### 2.1 典型业务场景

别名收束不是一个孤立的技术炫技，它直接影响下游多个业务任务的准确率：

**场景一：意图识别**

```
用户输入："帮我看看菊花厂最近的财报"

┌─────────────────────────────────────────────────────┐
│  没有别名收束：                                      │
│    模型不知道 "菊花厂" 是什么 → 意图无法匹配          │
│    → "财报查询" 意图 → 漏识别                        │
│                                                     │
│  有别名收束：                                        │
│    模型理解 "菊花厂" = 华为 → 归入 "企业财报查询"     │
│    → 意图命中 ✓                                      │
└─────────────────────────────────────────────────────┘
```

**场景二：内容分类**

```
用户生成内容："今天水果厂又挤牙膏了，新iPhone毫无亮点"

┌─────────────────────────────────────────────────────┐
│  没有别名收束：                                      │
│    "水果厂" 未命中任何产品词 → 分类到 "其他/闲聊"     │
│    → 丢失了一条有价值的竞品讨论                       │
│                                                     │
│  有别名收束：                                        │
│    "水果厂" = Apple, "挤牙膏" = 迭代保守             │
│    → 正确分类到 "竞品讨论 / Apple"                   │
└─────────────────────────────────────────────────────┘
```

**场景三：实体链接与知识图谱**

```
用户问："花厂和水果厂哪个待遇好？"

没有别名收束 → 两个实体都无法链接到知识库 → 无法回答
有别名收束   → 华为 vs Apple → 可查询知识库 → 可回答
```

### 2.2 这是一个共性问题

别名识别不敏感，不只是我们一家的痛点。这是所有面向 C 端用户文本的 NLP 系统都会遇到的问题：

| 行业 | 示例 |
|---|---|
| 汽车 | "大劳" = 劳斯莱斯、"蓝天白云" = 宝马、"四个圈" = 奥迪 |
| 消费品 | "肥宅快乐水" = 可口可乐、"蓝厂/绿厂" = vivo/OPPO |
| 金融 | "宇宙行" = 工商银行、"韭菜" = 散户、"大A" = A股 |
| 互联网 | "鹅厂" = 腾讯、"猪厂" = 网易、"开水团" = 美团 |
| 硬件 | "农企" = AMD、"牙膏厂" = Intel、"核弹厂" = NVIDIA |

> 本质上，**别名是用户真实语言和系统内部知识之间的鸿沟**。模型如果缺乏这方面的世界知识，所有下游任务的准确率都会打折扣。

### 2.3 为什么传统方法不够

| 方法 | 问题 |
|---|---|
| **规则/词典匹配** | 别名无限增长，维护成本爆炸；无法处理上下文歧义（"苹果"=水果还是Apple？） |
| **NER 模型** | 需要大量标注数据；新增别名需重新标注+重训；无法泛化到未见过的别名 |
| **检索增强（RAG）** | 依赖外部知识库的召回率；增加推理延迟；别名→本名映射库本身也要维护 |

这些方法各有各的问题，但它们都指向同一个本质：**这个能力应该内化到模型自身**，而不是外挂。

---

## 三、问题本质分析

### 3.1 别名收束到底是一个什么问题

在动手设计解决方案之前，需要先回答一个根本问题：**别名收束是什么性质的问题？**

```
┌──────────────────────────────────────────────────────────────┐
│                                                               │
│   别名收束 ≠ 一项"技能"（Skill）                               │
│   别名收束 = 一块"知识"（Knowledge）                           │
│                                                               │
│   ─────────────────────────────────────                       │
│                                                               │
│   技能型问题（如翻译、摘要、代码生成）：                         │
│     → 需要大量任务特定数据来塑造模型的行为模式                   │
│     → 训练不当容易挤压其他能力                                 │
│                                                               │
│   知识型问题（如知道巴黎是法国首都、水的沸点是100°C）：          │
│     → 只需要让模型"知道"这些事实                               │
│     → 模型已有的语言能力会自然地运用这些知识                    │
│     → 对模型行为模式的影响很小                                 │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### 3.2 举一个类比

> 一个懂中文的人，如果不知道"鹅厂"就是腾讯，他在阅读讨论腾讯的帖子时会感到困惑。但一旦有人告诉他"鹅厂=腾讯"，他所有的阅读理解能力、归纳总结能力、对话能力都能立刻运用这个知识。他不需要被重新"训练"如何对话——他只是获得了一条新知识。

模型同理。**模型不是不会收束，它只是不知道这些别名对应什么。** 一旦它"知道"了，它已有的语言理解能力足以处理"什么时候该用、什么时候不该用"。

### 3.3 为什么 SFT 重训不是最优解

如果我们把别名收束当成技能，用大量 SFT 对话数据来训练：

```
┌────────────────────────────────────────────────────────────┐
│  SFT 方案的代价（8-9B 模型，参数有限）                       │
│                                                             │
│  1. 大量别名专项 SFT 数据 → 模型学会 "遇到别名就收束" 的模式  │
│     → 这是一种行为层面的过拟合                                │
│                                                             │
│  2. 参数容量被分配给 "收束行为" → 其他能力的参数空间被挤压    │
│     → 推理、代码、对话等能力可能退化                          │
│                                                             │
│  3. 为了解决一个痛点（别名不识别），                          │
│     → 制造了更多痛点（其他能力下降）                          │
│                                                             │
│  4. 负例永远不够 → "刘德华为小明挤牙膏" 类问题防不胜防        │
│     → DPO 也只能覆盖已见过的模式，未见过的依然会误触          │
└────────────────────────────────────────────────────────────┘
```

> **核心矛盾**：8-9B 的参数量是一个零和空间，分配给"别名收束行为"的参数，必然来自其他能力。

### 3.4 我们应该怎么做：知识注入而非行为训练

```
┌────────────────────────────────────────────────────────────┐
│  正确的路径：Continue Pretraining（CPT）                     │
│                                                             │
│  目标：让 "菊花厂 = 华为" 成为模型的常识                     │
│                                                             │
│  手段：将包含别名-本名自然共现的领域文本，                    │
│        混入通用预训练语料中，做轻量 CPT                       │
│                                                             │
│  效果：                                                      │
│    • 模型知道了 "菊花厂" 就是 "华为"                         │
│    • 模型原有的语言能力会自然地运用这条知识                   │
│    • 不需要训练 "什么时候收束" —— 语言理解能力已经会判断     │
│    • 参数空间不因 "新技能" 而被挤压                          │
│    • 通用能力退化风险极小（因为只是扩充知识，不是重塑行为）   │
└────────────────────────────────────────────────────────────┘
```

### 3.5 小结：问题的技术定位

| 维度 | 判断 |
|---|---|
| 问题性质 | **世界知识缺失**，非能力缺失 |
| 解决方向 | **知识注入**（CPT），非行为训练（SFT） |
| 核心风险 | 如果用行为训练的方式解决知识问题，会挤压有限参数空间，导致通用能力退化 |
| 核心原则 | 信任模型已有的语言理解能力，只补充它缺失的知识，不教它"怎么做" |
| 数据原则 | CPT 阶段注入知识（95%+），SFT 阶段少量演示（<5%），保持通用数据占比 ≥70% |

---

## 四、方案选型

### 4.1 候选方案全景

解决"模型不认识别名"这个问题，业界有四类主流方案。我们从**效果、成本、对通用能力的影响、可扩展性**四个维度做对比。

---

#### 方案 A：外挂词典 / 规则匹配

```
用户输入 → 正则/词典匹配别名字段 → 替换为本名 → 送入模型
```

| 维度 | 评价 |
|---|---|
| 实现成本 | ★☆☆☆☆ 极低 |
| 别名覆盖率 | ★★☆☆☆ 只能覆盖已录入的，新增别名需人工维护 |
| 上下文歧义处理 | ★☆☆☆☆ 无法处理，"苹果"永远被替换或永远不替换 |
| 对模型能力影响 | 无（完全外挂，不涉及训练） |
| 长期可维护性 | ★☆☆☆☆ 别名库不断膨胀，歧义规则不断叠加，最终不可维护 |

> **结论**：可作为兜底手段，但不能作为主方案。歧义问题无解。

---

#### 方案 B：大量 SFT 数据训练别名收束能力

```
构造海量 "含别名输入 → 含本名输出" 的对话数据 → 全量 SFT
```

| 维度 | 评价 |
|---|---|
| 实现成本 | ★★★★☆ 需构造数万至数十万条专项 SFT 数据 |
| 别名覆盖率 | ★★★★☆ 训练数据中覆盖的别名可被识别 |
| 上下文歧义处理 | ★★★☆☆ 需要大量负例 + DPO 微调边界，且无法穷举 |
| 对模型能力影响 | ★★☆☆☆ **高风险**：专项能力过拟合，挤压其他能力 |
| 长期可维护性 | ★★★☆☆ 新产品/别名需重新构造数据 + 重训 |

> **结论**：可行但危险。相当于用一个 8-9B 模型的大量参数去学一个"别名替换器"，性价比极低，且对通用能力有显著退化风险。

---

#### 方案 C：RAG / 知识库检索增强

```
用户输入 → 检索别名知识库 → 将别名-本名映射注入 prompt → 模型推理
```

| 维度 | 评价 |
|---|---|
| 实现成本 | ★★★☆☆ 中等 |
| 别名覆盖率 | ★★★★☆ 知识库可随时更新 |
| 上下文歧义处理 | ★★★★☆ 检索结果可经模型语义匹配后决定是否使用 |
| 对模型能力影响 | 无（不涉及训练） |
| 长期可维护性 | ★★★★☆ 别名知识库独立维护 |

> **结论**：不错的补充方案，但不应该作为唯一手段。原因：(1) 增加推理延迟和成本；(2) 检索召回率受限，检索不到就无济于事；(3) 模型自身不理解别名，始终依赖外挂。

---

#### 方案 D：CPT 注入领域知识（推荐）

```
构建别名-本名自然共现的领域文本 → 混入通用语料做轻量 CPT → 知识内化为模型常识
```

| 维度 | 评价 |
|---|---|
| 实现成本 | ★★★☆☆ 中等（需构建领域 CPT 语料） |
| 别名覆盖率 | ★★★★☆ 与 CPT 语料覆盖度一致，可通过语料生成覆盖全产品树 |
| 上下文歧义处理 | ★★★★★ 不专门训练"收束行为"，依赖模型已有语言理解能力自然判断 |
| 对模型能力影响 | ★★★★★ **低风险**：知识扩充，非行为重塑 |
| 长期可维护性 | ★★★★★ 新产品/别名 → 生成新语料 → 轻量增量 CPT |

> **结论**：最适合当前约束的方案。模型获得了知识而非技能，参数空间不被挤压，通用能力退化风险最小。

---

### 4.2 方案对比总览

```
                    别名识别    上下文    通用能力    长期可
                      效果      歧义处理    保持      维护性
                      ────      ────      ────      ────
方案A: 规则匹配         ★★        ★         ★★★★★      ★
方案B: 大量SFT          ★★★★      ★★★       ★★         ★★★
方案C: RAG检索          ★★★★      ★★★★      ★★★★★      ★★★★
方案D: CPT知识注入      ★★★★      ★★★★★     ★★★★★      ★★★★★  ← 推荐
```

### 4.3 推荐策略：CPT 为主 + RAG 兜底

```
┌──────────────────────────────────────────────────────────┐
│                    分层策略                               │
│                                                           │
│  主路径（CPT 注入）：                                     │
│    模型自身知道别名 ≈ 本名 → 自动进行语义级收束            │
│    → 覆盖 90%+ 的常见别名场景                             │
│                                                           │
│  兜底路径（RAG 检索）：                                   │
│    长尾/新增别名 → 知识库检索 → prompt 注入               │
│    → 覆盖 CPT 语料未覆盖或模型未充分学会的别名             │
│                                                           │
│  最后防线（规则）：                                       │
│    高频、无歧义的别名 → 轻量词典 → 零成本兜底             │
└──────────────────────────────────────────────────────────┘
```

### 4.4 为什么排除了 DPO 优先级

DPO 在之前的讨论中被提及，这里明确其定位：

- **DPO 是一种行为微调手段**，在模型已经有了基本能力、需要精细调控"什么该做什么不该做"时效果最好
- 在当前阶段（模型根本不认识别名），DPO 是**跳过基础建设直接做精装**，效率很低
- **不排除未来引入 DPO**：当 CPT 注入知识后，若评测发现模型在特定边界 case 上仍有偏误，可用少量 DPO 数据做针对性修正
- 但这不是初版的目标，初版应专注于**把知识注入做好**

---

## 五、技术方案设计

### 5.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        训练流水线                            │
│                                                              │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌─────────┐ │
│  │ 产品树    │ →  │ CPT 语料  │ →  │ 轻量 CPT │ →  │  基座   │ │
│  │ 别名映射  │    │ 生成引擎  │    │ 知识注入  │    │ 模型 v1 │ │
│  └──────────┘    └──────────┘    └──────────┘    └────┬────┘ │
│                                                       │      │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌───┴────┐ │
│  │ 混合 SFT  │ ←  │ 通用数据  │ +  │ 少量别名  │ ←  │ 基座   │ │
│  │ 常规训练  │    │ (≥70%)   │    │ 数据(<5%) │    │ 模型v1 │ │
│  └─────┬────┘    └──────────┘    └──────────┘    └────────┘ │
│        │                                                     │
│        ▼                                                     │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐               │
│  │  评测    │ →  │ 通过？    │ →  │  交付    │               │
│  │  全维度  │    │ 通用能力  │    │  v1      │               │
│  │          │    │ 无退化    │    │          │               │
│  └──────────┘    └────┬─────┘    └──────────┘               │
│                       │ 未通过                               │
│                       ▼                                      │
│               ┌──────────────┐                               │
│               │ 调整数据配比  │                               │
│               │ 回退重训      │                               │
│               └──────────────┘                               │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 CPT 语料构建 —— 核心工程

这是整个方案最关键的一步。目标是生成**自然的、多样化的**领域文本，让别名和本名在合理的语境中共现。

#### 5.2.1 语料类型设计

| 语料类型 | 风格 | 占比 | 示例 |
|---|---|---|---|
| **百科类** | 客观、正式 | 30% | "菊花厂，即华为技术有限公司的民间俗称，成立于1987年……" |
| **行业评论类** | 半正式、分析性 | 25% | "最近菊花厂的Mate系列又出新品了，华为在高端市场的布局越来越激进……" |
| **社区讨论类** | 口语化、对话感 | 25% | "各位大佬，菊花厂和水果厂该选哪个？华为的信号好但Apple的生态香啊……" |
| **新闻资讯类** | 新闻体 | 10% | "据华为官方消息，这家被网友亲切称为'菊花厂'的科技巨头将于下月……" |
| **QA 问答类** | 问答体 | 10% | "问：花厂的笔记本怎么样？答：华为的笔记本近年来进步很大……" |

#### 5.2.2 语料生成流程

```
产品树（本名 + 别名列表）
        │
        ▼
┌─────────────────────────────────────┐
│  Step 1: 模板生成（覆盖全产品树）      │
│  • 每种语料类型设计 30-50 个句式模板   │
│  • 槽位填充：本名、别名、产品属性等     │
│  • 产出：保证每个产品-别名对至少出现   │
│          3-5 种不同语境的语料          │
│  • 量级：~10-20 万条                   │
└─────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────┐
│  Step 2: 强模型辅助生成（增加多样性）   │
│  • 用 Claude / GPT-4 级模型批量生成    │
│  • 提供产品信息和别名，要求生成自然文本  │
│  • Prompt 约束：                       │
│    - 文本中自然提及别名和本名           │
│    - 不要每句话都硬塞别名               │
│    - 模拟真实的写作/对话风格            │
│  • 产出：~3-5 万条高质量语料            │
└─────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────┐
│  Step 3: 质量过滤                     │
│  • 去重（语义级去重，非字符串匹配）     │
│  • PPL 过滤（剔除不通顺的模板产物）     │
│  • 别名覆盖率检查（确保无遗漏产品）     │
│  • 人工抽检 5%（重点检查生成质量）      │
└─────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────┐
│  Step 4: 混入通用 CPT 语料            │
│  • 领域语料 : 通用语料 = 3-5 : 95-97  │
│  • 确保领域语料不形成独立块，           │
│    而是均匀散布在通用语料中             │
└─────────────────────────────────────┘
```

#### 5.2.3 关键设计：不构造"负例 CPT 语料"

与传统方案不同，CPT 阶段**不专门构造负例语料**（如"刘德华为小明挤牙膏"类文本）。原因：

> CPT 的目标是注入知识，不是训练行为边界。模型在海量通用 CPT 语料中已经见过无数"字符串巧合但不是指称"的情况，它已有的语言理解能力足够处理这个问题。专门构造负例反而可能让模型对别名字符串产生"回避反应"，得不偿失。

---

### 5.3 SFT 阶段设计

#### 5.3.1 数据配比红线

```
SFT 训练数据总构成：

    ┌────────────────────────────────────────────┐
    │  通用能力保持数据         ≥ 70%            │
    │  （数学、代码、推理、对话、指令跟随等）     │
    ├────────────────────────────────────────────┤
    │  公司其他业务任务数据     ~25%             │
    │  （意图识别、内容分类等）                   │
    ├────────────────────────────────────────────┤
    │  别名相关数据             3-5%             │
    │  （仅做知识运用演示）                       │
    └────────────────────────────────────────────┘
```

**红线规则**：别名相关数据在任何情况下不超过 5%。宁可别名收束能力收敛慢一点，也不能让其他能力倒退。

#### 5.3.2 别名数据的融入方式

不创建独立的"别名收束任务"，而是将别名数据**分散嵌入**各类已有任务中：

| 任务类型 | 嵌入方式 | 示例 |
|---|---|---|
| 指令跟随 | 用户指令中含别名 | "帮我查一下菊花厂的财报" → 回复中使用"华为" |
| 内容摘要 | 待摘要文本含别名 | 原文提"花厂"，摘要中用"华为" |
| 对话 | 对话历史含别名 | 用户说"水果厂"，回复中自然称为"Apple" |
| 知识问答 | 问题中含别名 | "遥遥领先是哪款手机的外号？" |

所有别名数据**不单独标注**"此处发生了别名收束"——模型只需要正常完成任务，别名是否被正确收束体现在输入和输出的事实一致性上。

#### 5.3.3 SFT 数据量级

| 数据类型 | 量级 |
|---|---|
| 通用能力保持数据 | 已有数据保持，不削减 |
| 别名嵌入数据 | ~1000-3000 条（不是万条级别！分散在各类任务中） |

> 这个量级远小于之前纯 SFT 方案的计划。因为知识已经在 CPT 阶段注入了，SFT 阶段只需要少量样本演示"在对话中如何运用这个知识"。

---

### 5.4 能力保持机制

#### 5.4.1 全维度评测矩阵

每次训练（CPT 和 SFT）前后，跑完整的评测矩阵：

| 评测维度 | 评测集 | 权重 | 退化阈值 |
|---|---|---|---|
| 推理能力 | GSM8K / MATH 子集 | 高 | ≤ 2% |
| 代码能力 | HumanEval | 高 | ≤ 2% |
| 中文理解 | C-Eval / CMMLU 子集 | 高 | ≤ 2% |
| 通用知识 | MMLU 子集 | 中 | ≤ 3% |
| 指令跟随 | 内部评测集 | 高 | ≤ 2% |
| 对话质量 | 内部评测集（含人工评估） | 中 | 无明显退化 |
| **别名收束** | 自建评测集 | — | 目标提升 |
| **"牙膏测试"** | 对抗评测集 | — | 误触发率 ≤ 基准线 |

#### 5.4.2 训练中止与回退机制

```
训练前：记录全维度基线分数

CPT 完成后：
  ├── 别名收束提升 ✓ + 通用能力无退化 ✓ → 进入 SFT
  ├── 别名收束未提升 ✗ → 检查 CPT 语料质量，调整后重试
  └── 通用能力退化 > 阈值 ✗ → 降低领域语料占比，重新 CPT

SFT 完成后：
  ├── 全维度通过 ✓ → 发布
  └── 任一维度退化 ✗ → 回退 SFT 数据配比，重训
```

#### 5.4.3 消融实验先行

正式训练前必须完成消融实验，确认关键超参：

| 消融实验 | 变量 | 目标 |
|---|---|---|
| CPT 语料占比 | 1% / 3% / 5% / 10% 领域语料 | 找到"刚好够用"的最小混入比例 |
| CPT 步数 | 1K / 3K / 5K steps | 找到知识注入充分但不发生过拟合的步数 |
| SFT 别名数据占比 | 1% / 3% / 5% | 验证通用能力退化曲线 |

---

### 5.5 评测集设计

#### 5.5.1 别名收束评测集（自建）

| 子集 | 说明 | 量级 |
|---|---|---|
| **正例集** | 上下文中别名明确指代产品，检验是否正确收束 | ~500 条 |
| **负例集（牙膏测试）** | 别名字符串出现但不指代产品的句子，检验是否误触发 | ~200 条 |
| **歧义集** | 上下文不足以判断，检验模型是否保持谨慎（不回退为乱猜） | ~100 条 |
| **长尾集** | 低频别名、新造别名，检验泛化能力 | ~200 条 |

#### 5.5.2 评测指标

| 指标 | 定义 | 目标 |
|---|---|---|
| **收束准确率** | 正例集中别名→本名归一化的准确率 | ≥ 85%（初版） |
| **误触发率** | 负例集中错误触发收束的比例 | ≤ 5%（初版） |
| **歧义保守率** | 歧义集中模型不强行收束的比例 | ≥ 80% |
| **通用能力退化率** | 通用评测维度相对基线的退化幅度 | 所有维度 ≤ 2% |

---

## 六、执行计划

### 6.1 初版交付：三周计划

```
Week 1                    Week 2                    Week 3
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
│ CPT 语料工程     │      │ CPT 正式训练     │      │ SFT 正式训练     │
│ + 评测集搭建    │  →   │ + 消融实验       │  →   │ + 全维度评测     │
│ + 消融实验启动  │      │ + SFT 数据准备   │      │ + 修复 + 交付    │
└─────────────────┘      └─────────────────┘      └─────────────────┘
```

#### 第一周：数据基建 + 评测集 + 消融实验

| 天 | 事项 | 产出 | 备注 |
|---|---|---|---|
| D1-2 | 产品树 → 结构化别名映射库 | 知识库（本名+别名+产品属性） | 输入：产品团队提供的产品树 |
| D2-3 | CPT 语料生成引擎开发 | 模板系统 + LLM 生成 pipeline | 覆盖全部产品-别名对 |
| D3-5 | CPT 语料批量生成 + 质量过滤 | ~10-20 万条高质量领域语料 | 模板+强模型生成，PPL 过滤 |
| D5-6 | 评测集构建 | 别名收束评测集（正例/负例/歧义/长尾） | 含"牙膏测试"类对抗样本 |
| D6 | 通用能力基线录制 | 全维度评测基线分数 | 训练前必须记录 |
| D7 | 小规模 CPT 消融实验启动 | 1%/3%/5% 领域语料占比对比 | 用 1/10 数据量快速验证 |

**第一周里程碑**：
- ✅ 领域 CPT 语料就绪（≥10 万条，覆盖全产品树）
- ✅ 评测集就绪（别名收束 + 通用能力）
- ✅ 消融实验结果：确定最优 CPT 语料占比和步数

---

#### 第二周：CPT 正式训练 + SFT 准备

| 天 | 事项 | 产出 | 备注 |
|---|---|---|---|
| D8-9 | CPT 正式训练 | 基座模型 v1-CPT | 基于消融实验确定的最优超参 |
| D9-10 | CPT 后评测 | 别名收束变化 + 通用能力变化 | 通用能力退化 > 阈值则调整重来 |
| D10-12 | SFT 数据准备 | 混合 SFT 数据（通用 70%+别名 3-5%） | 别名数据分散嵌入各类任务 |
| D12-13 | 小规模 SFT 消融实验 | 确认 SFT 数据配比最优值 | 不同别名数据占比的退化曲线 |
| D13-14 | SFT 试训练 + 评测 | v0 内部版本 | 全维度评测 |

**第二周里程碑**：
- ✅ CPT 训练完成，别名知识成功注入，通用能力无退化
- ✅ SFT 数据就绪，消融确认配比
- ✅ v0 版本可用于内部评估

---

#### 第三周：SFT 正式训练 + 打磨交付

| 天 | 事项 | 产出 | 备注 |
|---|---|---|---|
| D15-16 | SFT 正式训练 | 基座模型 v1-SFT | 基于消融确定的最优配比 |
| D16-18 | 全维度评测 | 完整评测报告 | 包含与基线模型的对比 |
| D18-19 | Badcase 修复 | 针对性数据补充 + 微调 | 重点是误触发和漏收束 case |
| D19-20 | 对抗测试 + 最终评测 | 最终评测报告 | 含人工抽检 |
| D20-21 | 文档编写 + Buffer | 交付包：模型 + 评测报告 + 数据配方 | 处理意外情况 |

**第三周里程碑**：
- ✅ v1 模型全维度评测通过（别名收束提升 + 通用能力无退化）
- ✅ 评测报告完整
- ✅ 数据配方文档归档，支撑后续迭代

---

### 6.2 初版目标设定

| 指标 | 初版目标 | 说明 |
|---|---|---|
| 别名收束准确率 | ≥ 85% | 正例集中别名→本名的归一化准确率 |
| 误触发率 | ≤ 5% | "牙膏测试"类负例的误触发比例 |
| 通用能力退化 | 所有维度 ≤ 2% | 硬性红线，不妥协 |
| 产品覆盖率 | 全产品树 | CPT 语料覆盖每一个产品-别名对 |

> 初版不追求 95%+ 的收束准确率。目标是**在不损伤通用能力的前提下，建立基本的别名收束能力**，为后续迭代打好地基。

---

### 6.3 后续迭代路线图

```
初版 v1（3 周）           v1.1（+2 周）           v1.2（+4 周）          v2（+8 周）
     │                        │                      │                     │
     ▼                        ▼                      ▼                     ▼
┌──────────┐           ┌──────────┐           ┌──────────┐           ┌──────────┐
│ CPT 注入  │           │ 生产环境  │           │ 标注数据  │           │ 引入 DPO │
│ 基础别名  │    →      │ 反馈回流  │    →      │ 质量提升  │    →      │ 精调边界 │
│ 知识      │           │ 数据飞轮  │           │ 增量训练  │           │ + RAG    │
└──────────┘           └──────────┘           └──────────┘           └──────────┘
```

#### v1.1（初版后 +2 周）：数据飞轮启动

| 事项 | 说明 |
|---|---|
| 生产环境部署 | v1 模型上线，低置信度样本回流 |
| 错误分析 | 收集生产环境中漏收束 / 误触发的 case |
| 评测集扩充 | Badcase 自动加入评测集，防止回退 |
| 增量 CPT 语料 | 补充高频错误对应的别名语料 |
| 增量训练 | 基于扩展后的 CPT 语料做轻量增量训练 |

#### v1.2（初版后 +6 周）：标注数据融合

| 事项 | 说明 |
|---|---|
| 标注数据清洗 | 标注团队约万条数据经冲突检测后入训 |
| 评测集金标化 | 用高质量标注数据构建更精准的评测集 |
| CPT + SFT 混合增量训练 | 标注数据 + 语料数据 + 生产反馈数据融合 |
| 收束准确率目标 | ≥ 92% |

#### v2（初版后 +12 周）：能力成熟

| 事项 | 说明 |
|---|---|
| DPO 引入 | 当基础收束能力稳定后，用 DPO 精调边界 case |
| RAG 集成 | 为长尾 / 新造别名提供检索兜底 |
| 多任务联合优化 | 与公司其他业务任务联合评测，确保整体最优 |
| 收束准确率目标 | ≥ 95%，误触发率 ≤ 2% |

---

### 6.4 可持续迭代的基础设施

```
┌────────────────────────────────────────────────────────────┐
│                      数据飞轮架构                           │
│                                                             │
│  ① 生产环境                                                 │
│     ├── 模型推理（含置信度标记）                             │
│     └── 低置信度样本自动回流                                 │
│           │                                                 │
│           ▼                                                 │
│  ② 数据工厂                                                 │
│     ├── 冲突检测 → 标注审核 → 入训                          │
│     ├── 新别名发现 → CPT 语料自动生成                       │
│     └── Badcase → 评测集自动扩充                            │
│           │                                                 │
│           ▼                                                 │
│  ③ 训练流水线                                               │
│     ├── 增量 CPT（新语料混入）                               │
│     ├── 增量 SFT（新标注数据混入）                           │
│     └── 全维度评测 Gate（通过 → 发布，不通过 → 回退）        │
│           │                                                 │
│           ▼                                                 │
│  ④ 模型仓库                                                 │
│     ├── 版本化管理（每版保留完整训练配置和评测结果）          │
│     └── A/B 部署 → 对比评测 → 全量上线                      │
└────────────────────────────────────────────────────────────┘
```

这个架构确保：
- 每次迭代的**成本递减**（不需要从头造数据）
- **评测集持续生长**（每发现一个 badcase 就固化，永不回退）
- **新产品/新别名**的接入成本趋近于零（只需更新产品树 → 自动生成语料 → 增量训练）
- **版本可追溯**（任何时间点可以回退到任意历史版本）

---

## 七、风险与应对

### 7.1 风险矩阵

| # | 风险 | 概率 | 影响 | 等级 | 应对策略 |
|---|---|---|---|---|---|
| R1 | **CPT 知识注入不充分**：训练完模型仍不认识部分别名，收束准确率不达标 | 中 | 高 | 🟡 | ① 消融实验提前验证注入效果；② 检查 CPT 语料中别名出现的频率和语境多样性，补强薄弱产品；③ 降低初版目标至 75-80%，后续迭代持续提升 |
| R2 | **通用能力退化**：CPT 或 SFT 后推理/代码/对话等能力下降超过阈值 | 低 | 极高 | 🔴 | ① 严格遵循数据配比红线（领域语料 ≤5% CPT，别名数据 ≤5% SFT）；② 全维度评测 Gate，不通过绝不发布；③ 消融实验先行，提前探明安全区间；④ 退化 → 降低领域占比重新训练 |
| R3 | **长尾别名覆盖不足**：产品太多，部分产品的低频别名 CPT 语料难以充分覆盖 | 高 | 中 | 🟡 | ① 模板生成确保每个别名至少 3-5 种语境覆盖；② 初版接受长尾别名效果稍差，用 RAG 兜底；③ 数据飞轮运行后，生产环境回流的长尾 case 自然补充 |
| R4 | **标注数据质量问题**：标注团队提交的数据冲突、错漏多，无法直接入训 | 中高 | 中 | 🟡 | ① 冲突检测 pipeline 自动标注存疑数据；② 多标注员一致性作为数据质量评分，低分数据降权或丢弃；③ 不依赖标注数据做初版——初版主要靠 CPT 语料驱动，标注数据留到 v1.2+ 融合 |
| R5 | **"牙膏测试"类误触发**：模型对某些别名字符串过度敏感，在非指称语境中错误收束 | 中 | 中 | 🟡 | ① 评测集中"牙膏测试"类负例作为硬性指标；② 若误触发率超标，分析错误模式，少量精准 SFT 数据修正；③ v2 阶段引入 DPO 精调边界 |
| R6 | **基座模型变更**：公司更换基座模型（如从 Qwen 换到 DeepSeek），已投入的 CPT 语料是否可复用 | 低 | 中 | 🟢 | CPT 语料与基座无关（纯文本），可直接复用。只需在新基座上重新跑 CPT + SFT，数据成本零 |
| R7 | **工期压缩**：三周时间被压缩到两周或更短 | 低 | 高 | 🟡 | ① 优先保证 CPT + 评测集，SFT 可简化（减少消融实验，用经验值替代）；② 降低初版交付标准（只交付 CPT 版本，SFT 放到下一轮） |

### 7.2 最高优先级风险专述

#### R2：通用能力退化（等级：🔴 极高）

这是整个方案最大的风险，必须单列强调：

```
为什么这个风险是最高优先级？

1. 8-9B 的参数量是硬约束。每一次训练都会重新分配参数空间。
2. 如果为了解决"别名不识别"这一个业务痛点，
   导致模型在其他五个维度上退化，
   那就不是解决问题，而是转移问题。

3. 用户不会因为模型能识别别名而感到惊喜，
   但会因为模型变笨了而感到愤怒。

应对原则：
┌──────────────────────────────────────────────────────────┐
│  宁可别名收束能力提升慢一点，也绝不能拿通用能力换。       │
│                                                          │
│  CPT 数据配比红线：领域语料 ≤ 5%                          │
│  SFT 数据配比红线：别名数据 ≤ 5%                          │
│  退化阈值：任一通用维度 ≤ 2%                              │
│                                                          │
│  突破任何一条红线，训练必须回退。                          │
└──────────────────────────────────────────────────────────┘
```

### 7.3 风险缓解的工程手段

| 机制 | 说明 |
|---|---|
| **消融实验前置** | 正式训练前用 1/10 数据量验证方向正确性，避免全量训练的浪费和风险 |
| **训练检查点（Checkpoint）** | CPT 训练保留多步检查点，发现后期退化可回退到最优步数 |
| **评测 Gate** | 设置自动化评测关卡，不通过无法进入下一阶段，杜绝"带着问题继续" |
| **A/B 部署** | 新模型先灰度 10% 流量，对比旧模型在真实场景下的表现，确认无退化再全量 |
| **版本回退** | 保留历史版本的完整训练配置和评测结果，任何版本可一键回退 |
