# SFT 知识使用：三层激活方案

- 日期：2026-09-14
- 背景问题：知识注入后使用侧仍有 gap。① 社媒场景中人们说话与知识重合度低（别名/黑话/特征描述代替官方名）；② 学会知识 ≠ 会用于判断——system prompt 里写"识别到华为耳机就是非噪声"依然误判一堆，因为规则的前提"识别到华为耳机"本身悬空（老模型不认识新产品，社媒也不叫官方名）。
- 配套：《SFT知识评测-插拔结构与最小闭环.md》《SFT评测结果-分析指南.md》

## 一、三层分工（核心方案）

"激活"要从任务里剥出来：实体识别/属性查取是任务无关的，做成一次性底座；每个任务只补一层薄薄的桥接示范。

| 层 | 内容 | 材料 | 成本 |
|---|---|---|---|
| **L1 激活层** | 社媒表面形式 → 实体/属性对齐（别名、指称、特征描述）。子问题："这条文本里有没有华为产品、哪款、哪个属性"——去噪要用，意图判断也要用，与任务形态无关 | 别名表 + 2-1 反向指代题的题面换成社媒原句（"星闪耳机这代降噪真顶" → FreeBuds Pro 5） | **做一次，摊销** |
| **L2 桥接层** | 每任务 200–500 条"贴文 → 依据 → 标签"。依据句必须同时出现：产品/属性 + 任务判断的连接。教的知识量≈0，示范的是**决策模式**："这个任务里知识该在哪个位置出场" | 真实样本 + LLM 标注依据句 + 人工抽检 | **每任务 1–2 天** |
| **L3 任务层** | 其余训练数据 | —— | **照旧，一条不动** |

桥接样本示例：

> 贴文："夹耳那个真的绝，办公室神器"
> 依据：提到 FreeClip 系列的佩戴体验，属于华为耳机产品讨论
> 标签：非噪声

要点：

1. 不是"每任务造示例 vs 每次全量重造 CoT"的二选一。L1 打底、L2 薄补，侵入性比全量重造小一个量级。
2. "只造示例担心效果不够"的担心是对的——底座是老模型时，只有桥接样本是空中楼阁（模型连"FreeClip 2 是耳机"都不知道，依据写不出来）。**L1 是 L2 的前提**。
3. 新增任务形态只付 L2 的钱。
4. 这就是当初拔掉的"指代模块"在社媒场景插回来的样子。

## 二、2×2 对照实验（先跑，四臂）

| 臂 | 知识注入（L1） | 特标桥接数据（L2） | 读到什么 |
|---|---|---|---|
| A 基线 | ✗ | ✗ | 老模型原始水平 |
| B | ✓ | ✗ | 知识进去了，但会不会用于判断？ |
| C | ✗ | ✓ | 想用知识，但没知识可用 |
| D | ✓ | ✓ | 目标态 |

读法：

- **D ≫ B 且 D ≫ C** → 知识是前提、桥接是放大器，两者缺一不可，三层路线成立；
- **B 都很差** → 知识注入本身没生效，回《SFT评测结果-分析指南.md》查训练侧；
- **D ≈ B** → 桥接样本没教会决策模式，查 L2 样本质量（依据句是否真的连上判断）。

## 三、效果不够好时的路由表

| 错误分布 | 缺哪层 | 补什么 |
|---|---|---|
| 标准名贴文也错 | L1 没生效 | 回知识注入评测，查训练侧 |
| 标准名对、别名/特征错 | L1 的别名层薄 | 挖更多社媒指称，扩 2-1 题面 |
| 都还行、边界样本错 | L2 多样性不足 | 补决策模式变体样本 |

跑完四臂，把 D 的误判样本按此表分层——改哪里就是查表的事。
