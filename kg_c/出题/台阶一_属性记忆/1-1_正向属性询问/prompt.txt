给定一个三元组（主语、谓语、宾语），生成一条自然语言问答对。

规则：
- 题干指向主语，问的是谓语，答案是宾语
- 题干和答案都必须无歧义，连起来读语义通顺
- 答案就是宾语本身，一个字都不许改
- 必须严格按照下方指定的问句模板填空，不得更换句式

问句模板：{TEMPLATE}

三元组：
主语: {S}
谓语: {R}
宾语: {O}

只输出 JSON，不要任何解释：
{"question": "...", "answer": "..."}
