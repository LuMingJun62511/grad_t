你的任务是为「换代产品对比」生成一条自然语言问答对，用于评测 AI 助手能否对比新旧两代产品的属性变化。

设计意图：
- 题干给出新旧两代产品，问新一代相比上一代在某属性上的变化/升级
- 题干中明确新旧方向（"相比""较上代""升级"）
- 考点是"跨代变化"，答案是变化描述（"从旧值升级为新值"）
- 只对比两代值不同的属性；答案只陈述变化，不吹捧

输入数据（JSON）：
{PKG}

只输出 JSON，不要任何解释：
{"question": "...", "answer": "..."}
