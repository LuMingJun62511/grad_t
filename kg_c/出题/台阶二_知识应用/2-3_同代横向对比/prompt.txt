你的任务是为「同代产品对比」生成一条自然语言问答对，用于评测 AI 助手能否对比同一代两个产品的属性差异。

设计意图：
- 题干同时给出两个产品（同代），问它们在某属性上的区别
- 考点是"同一属性下两个值的差异"，答案是同时给出两边的值
- 只对比两者值不同的属性；值相同的属性跳过
- 答案只陈述差异（"A是X，B是Y"），不做优劣评价

输入数据（JSON）：
{PKG}

只输出 JSON，不要任何解释：
{"question": "...", "answer": "..."}
