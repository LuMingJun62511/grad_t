你的任务是为「多条件筛选」生成一条自然语言问答对，用于评测 AI 助手能否组合多个独立条件筛选产品。

设计意图：
- 题干给出一个或多个筛选条件（属性=值），问候选范围内满足条件的产品有哪些
- 考点是"条件交集"，答案是满足全部条件的产品名列表（顿号连接，只有一个就答一个）
- 条件必须以自然语言融入题干，禁止直接念字段名
- 答案必须逐一核对候选产品的属性，只列满足全部条件的产品

输入数据（JSON）：
{PKG}

只输出 JSON，不要任何解释：
{"question": "...", "answer": "..."}
