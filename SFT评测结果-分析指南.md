# SFT 评测结果分析指南（照做版）

- 日期：2026-09-10
- 用途：拿到评测结果后照做。每一步 = 一个动作 + 一个读数 + 一个分支。Excel 就能算，不用写代码。
- 重要：文中所有数字都是编的示例，只演示结构和读法；照做时把真实数字填进去替换。
- 配套：《SFT知识评测设计评审.md》《SFT知识评测-插拔结构与最小闭环.md》

## 第 0 步：备料（四样东西）

1. **训练集结果**：拿训后模型跑一遍训练集，记准确率；
2. **评测 A 结果**（同形式题：1-1/2-1/1-3）：训前 + 训后，每题带题型标签、题目原文、标准答案、模型回答；
3. **评测 B 结果**（组合题：2-x）：同样格式；
4. **训练 input**（倒查元组用）。

另做一件事：**确认判分方式**。数字近似（5000 vs 5100）给不给分？集合题答对 2/3 给不给分？判分严不严会影响下面所有数字。

## 第 1 步：训练集准确率（一劈两半）

**动作**：算训后模型在训练集上的准确率。

**示例**：训练集 96%。

**怎么读**：

| 训练集准确率 | 结论 | 下一步 |
|---|---|---|
| ≥ 90% | 背下来了 | 走第 2 步 |
| ≤ 70% | 连背都没背下来 | 走 1A 训练侧清单 |
| 中间 | 背了一半 | 1A 清单和第 2 步都查 |

**1A 训练侧清单**（训练集低时逐项排查）：

- [ ] LoRA rank 调大试一轮（8 → 64/128）；
- [ ] 训练 loss 曲线收敛了吗？没收敛 → 加 epochs；
- [ ] lr 是否合适；
- [ ] 数据量是否超出 LoRA 容量（几百条 → 几千条，rank 要跟着涨）；
- [ ] 训练数据是否套了底座的 chat 格式（没套 = 容量浪费在学格式上）；
- [ ] 数字答案是否被严格判 0（很多"接近但差一位"的答案）。

## 第 2 步：按题型拆（平均数会骗人）

**动作**：把评测 A 的 60% 按题型分组报数。

**示例**：

| 题型 | 训后准确率 |
|---|---|
| 1-1 正向 | 76% |
| 2-1 反向 | 44% |
| 1-3 枚举 | 60% |
| 平均 | 60% |

**怎么读**：平均数 60% 藏着一个 44%。2-1（反向）远低于 1-1（正向）→ **方向反转门槛**：训练教正向、评测考反向，反向本来就难，这不是知识没进去。修法：训练里调高 2-1 占比，或单独加训 2-1。

1-3 也低 → 枚举答案长、评分严，单独看，别和 1-1 混着报。

## 第 3 步：按实体拆（新老分开）

**动作**：把每题按**训前对错**分成两类——训前对 = 老实体题（模型本来就会），训前错 = 新实体题（模型不知道）。两组分开报数。

**示例**（2-1 的 100 题）：

| 组 | 题数 | 训后准确率 |
|---|---|---|
| 新实体（训前错） | 76 | 49% |
| 老实体（训前对） | 24 | 29% |

**怎么读**：

- 新实体差 → 记忆容量/训练量问题，回第 1 步 1A 清单；
- **老实体差 → 预训练先验在干扰，冲突嫌疑**，走第 4 步。

## 第 4 步：冲突筛查（两种冲突，两个动作）

**4a 退化题（预训练冲突的指纹）**

动作：筛出"**训前对、训后错**"的题，只倒查这些题的元组。

示例：找出 21 道退化题，其中 18 道属于老实体 → 重点查这 18 道对应的三元组：SFT 数据和底座先验在哪里打架。

> 注意：冲突的指纹不是"没学会"，而是"训后比训前还差"。你挑题时已排除了模型知道的题，所以普通错题不该甩锅给冲突——只有退化题才配。

**4b 数据内部冲突（全量扫，值得做）**

动作：扫训练 input：

- [ ] 同一个 S+R（实体+关系）出现两个不同的 O？
- [ ] 2-1 要求的唯一性被破坏？某个值在 KG 里命中 > 1 个实体？

示例：扫出 3 个 S+R 有两个 O → 这 3 条相关的题训后必乱，修数据。

## 第 5 步：错题审计（抽 50 条分四类）

**动作**：从评测 A 错题里随机抽 50 条，每条归入四类之一。

**示例**：

| 类别 | 样子 | 条数 |
|---|---|---|
| 幻觉 | 问 Mate 80 Pro 芯片，答了完全不相关的东西 | 12 |
| 兄弟混淆 | 答成相近机型/相近值（Mate 80 答成 Mate 80 Pro Max） | 24 |
| 数字近似 | 5000 答成 5100 | 10 |
| 拒答/格式错 | 不答，或输出格式不对 | 4 |

**怎么读**（四类四个方向）：

| 最多的一类 | 指向 |
|---|---|
| 幻觉 | 没记住 → 训练侧（1A 清单） |
| 兄弟混淆 | 唯一性/干扰 → 做 4b 扫查 + 上硬负例 |
| 数字近似 | 判分太严 → 容错评分 |
| 拒答/格式错 | 模板/格式对齐 → 修 prompt 或格式 |

示例结论：兄弟混淆最多 → 优先做 4b 全量扫查，出题时加硬负例（评审问题 4）。

## 第 6 步：评测 B 的天花板（先算后怪）

**动作**：把每类组合题拆成"跳"，每跳取评测 A 对应题型的准确率，相乘 = 理论天花板。

**示例**：

- 2-3 对比题 = 取出 A 的属性（1-1 跳）× 取出 B 的属性（1-1 跳）× 找差异（估 0.9）
  → 0.76 × 0.76 × 0.9 ≈ **0.52**。实测 2-3 只有 20% → 远低于天花板。
- 2-2 筛选题 = 反向跳（2-1 的 0.44）+ 集合完整性 → 上限约 0.44。实测 12% → 同样远低于。

**增量痕迹**（一个佐证）：评测 A 增量 40 个点，评测 B 增量 10 个点。10/40 ≈ 0.25 ≈ 0.5²——两跳各只转移了一半。

**怎么读**：

- 实测 ≈ 天花板 → 短板在单跳，先把第 2–5 步的短板修掉，B 自动跟着涨；
- **实测远低于天花板 → 组合能力本身没训练过**（训练集只有单三元组题）。修法：训练集掺少量 2-x 形式的题，再测一轮。

## 第 7 步：汇总成一张表

把上面所有观察汇成"观察 → 结论 → 动作"表。照做时把真实数字填进空模板：

| 观察（真实数字） | 结论 | 动作 |
|---|---|---|
| 训练集准确率 ___ | 背下来了吗 | 决定走 1A 还是第 2 步 |
| 1-1 ___ / 2-1 ___ | 反向是不是硬门槛 | 调 2-1 训练占比 |
| 新实体 ___ / 老实体 ___ | 容量问题还是冲突 | 决定走 1A 还是第 4 步 |
| 退化题 ___ 道 | 冲突规模 | 倒查这些题的元组 |
| 审计：___ 类最多 | 主错误模式 | 按第 5 步表格选动作 |
| B 实测 ___ / 天花板 ___ | 组合有没有被训练 | 决定掺不掺 2-x 题 |

## 附：一个一天能出的 ablation

去掉 1-3，只训 1-1 + 2-1，跑一遍看 1-1/2-1 评测涨不涨。1-3 的答案是"列表"，和单值答案结构不同，可能干扰记忆。涨了 → 1-3 出训练集；没涨 → 保留原配置。
