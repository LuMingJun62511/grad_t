# Rerank 模型增训方案（汇报版）

> 一句话摘要：检索器召回 10 条结果，用 Qwen3-Reranker-0.6B 增训一个排序模型，把"想要的"排前面、"不想要的"排后面。训练形态与标准 SFT 同构，复用公司现有 swift 管线，单卡 910B4 即可训练和部署。

## 1. 背景与总体路线

**业务场景**：检索器每次召回 10 条候选，业务希望"想要的"排在前面、"不想要的"排在后面；想要与不想要之间的内部顺序不重要。

**关键数字**：

| 项 | 值 |
|---|---|
| 基座模型 | Qwen3-Reranker-0.6B（官方 rerank 模型，增训） |
| 数据 | 600 个 query × 10 条候选 = 6000 个标注样本 |
| 训练框架 | ms-swift（公司已有环境，昇腾适配） |
| 算力 | 单卡 910B4（训练 + 部署均验证可行） |
| 训练形态 | pointwise 生成式（输出 yes/no） |

## 2. 为什么训练形态与标准 SFT 同构

Pointwise 形态：把 query 和 doc 用官方特殊标签拼进 prompt，模型输出一个词 `yes` 或 `no`。

**一条训练数据长这样**：

```
<Instruct>: 判断下列文档是否与查询相关
<Query>: 华为Mate60 电池容量
<Document>: 华为 Mate 60 内置 4750mAh 电池，支持 66W 有线快充…
→ 输出: yes
```

**与普通 SFT 并排对比**：

| | 普通 SFT | Rerank pointwise |
|---|---|---|
| 输入 | "华为Mate60电池容量是多少" | "`<Query>`:… `<Document>`:…" |
| 要生成的答案 | "4750mAh，支持66W快充" | "yes"（或 "no"） |
| loss | 对答案里每个 token 算交叉熵 | 对 "yes" 这 1 个 token 算交叉熵 |

**结论**：唯一的区别是"要生成的答案"从长文本变成了一个词。loss 无需自研，直接复用标准 SFT 管线。"需要自己写 loss"的是 pairwise/listwise 形态，本方案不涉及（列为后续升级项）。

## 3. 训练与推理方案

### 3.1 数据格式（喂给 swift 的 jsonl）

```json
{"messages": [
  {"role": "user", "content": "<Instruct>: 判断下列文档是否与查询相关\n<Query>: 华为Mate60 电池容量\n<Document>: 华为 Mate 60 内置 4750mAh 电池…"},
  {"role": "assistant", "content": "yes"}
]}
```

注意：`<Instruct>` `<Query>` `<Document>` 是官方模型的特殊标签，照抄不改。

### 3.2 训练命令骨架

```bash
ASCEND_RT_VISIBLE_DEVICES=0 swift sft \
  --model Qwen/Qwen3-Reranker-0.6B \
  --dataset rerank_train.jsonl \
  --train_type lora \
  --num_train_epochs 2 \
  --output_dir output/qwen3-reranker-voc-v1
```

（模型路径用 ModelScope 镜像；参数名以实际 swift 版本为准。）

6000 条 + 单卡，一轮训练分钟级——试错循环快是本路线的核心价值。

### 3.3 推理打分

不生成文本，直接取模型在 `yes` token 上的概率 `P(yes)` 作为每条候选的分数，10 条按分数排序。同一个模型两种用法：**训练走生成（白嫖标准 loss），推理取 logit（又快又稳）**。

## 4. 数据分层与负例挖掘方法

### 4.1 难易的定义：标签与模型分数的分歧程度

用当前模型给全部样本打一遍 `P(yes)`，每条落在四类之一：

| doc | 标签 | P(yes) | 归类 | 直觉 |
|---|---|---|---|---|
| 官方参数页 | yes | 0.97 | 简单正例 | 模型和业务一致 |
| 拆机评测（表达绕） | yes | 0.61 | 难正例 | 模型在犹豫 |
| Mate50 的参数 | no | 0.83 | 难负例 | 模型差点当成想要的 |
| 营销软文（没提容量） | no | 0.02 | 简单负例 | 一眼排除 |

标签和分数一致 = 简单，打架 = 难。两个分布的**重叠区就是边界**。

### 4.2 三步循环（最小可跑）

- **Step 1：打一遍分**（6000 条 × 0.6B，单卡分钟级）

```python
for row in data:
    row["p_yes"] = get_p_yes(row["query"], row["doc"])  # 取yes token的logit
df.to_csv("scored.csv")

# 看打脸样本
df[df.label=="no"].sort_values("p_yes", ascending=False).head(50)  # 最难负例
df[df.label=="yes"].sort_values("p_yes").head(50)                  # 最难正例

# 看分布重叠
df[df.label=="yes"].p_yes.hist(alpha=0.5)
df[df.label=="no"].p_yes.hist(alpha=0.5)
```

- **Step 2：人看打脸样本，归纳类型**——把最难负例 top50 和边界区（P 在 0.3~0.7）拿出来逐条看，总结负例特征。
- **Step 3：按归纳的类型造负例，进下一轮训练**——每转一圈，正负分布分开一点，重叠区变小，即"边界在收窄"。

### 4.3 负例类型学（模板，用实际数据填充）

| 类型 | 例子 | 制造源 |
|---|---|---|
| 相邻型号 | Mate50 参数混进 Mate60 查询 | 语料库 BM25 挖同品牌其他型号 |
| 过期/旧版 | 相关型号但参数已更新 | 版本对照，保留旧内容 |
| 泛泛软文 | 提了关键词但无实质信息 | 广告语料库 |
| 跨品类 | 别的电子产品 | 随机挖，最简单 |
| 格式垃圾 | 重复、截断、乱码 | 语料清洗副产品 |

从 top50 打脸样本归纳出的**本域特有类型**，是数据挖掘阶段的核心交付。

## 5. 效果评测与数据产出

**主指标：组序命中率**——"想要的全部排在不想的前面"的 query 占比。

**边界收窄指标**（每轮训练产出一组数，形成曲线）：

- 正/负例分数分布直方图并排对比（重叠区肉眼可见变小）
- 难负例比例：负例中 P(yes)>0.5 的占比
- 难正例比例：正例中 P(yes)<0.5 的占比

## 6. 实施节奏

| 阶段 | 内容 | 目标 |
|---|---|---|
| 冒烟测试 | 20 个 query（约 200 条）走通全链路 | 验证特殊标签不被管线吞、训练收敛 |
| 第一版 | 6000 条训练 + 组序命中率评测 | 管线跑通 + 摸清负例规律 |
| 迭代 | 难负例挖掘循环 2-3 轮 | 边界收窄，指标提升 |

**升级路径（每步互不牵连，按需启用）**：0.6B→4B（换权重，管线不动）；pointwise→pairwise（此时才涉及自写 loss）；简单负例→难负例。

## 7. 风险与依赖

| 风险 | 应对 |
|---|---|
| 业务标注量 6000 条，低于原计划 2 万 | 第一版定位为管线验证；负例主要靠挖掘自造（检索训练惯例：正例靠标、负例靠挖） |
| 特殊标签被训练管线清洗 | 冒烟测试第一项验证内容 |
| 正负比失衡（正例可能仅一千多条） | 先统计；失衡则正例 oversample 或 loss 加权，防止学成"全说 no" |
| 框架选择 | 不用 MindSpeed（场景错配：0.6B + 6000 条单卡即可，Megatron 系格式转换成本大于收益）；用 ms-swift，官方 Qwen-Rerank 微调代码仅作格式对齐参照 |

## 附：一分钟汇报口径

**路线**：用 Qwen3-Reranker-0.6B 官方 rerank 模型，在业务标注数据上做 SFT 增训，让召回结果按业务偏好排序（想要的靠前、不想要的靠后）。

**可行性**：rerank 的 pointwise 训练形态与标准 SFT 完全同构（区别仅是输出从长文本变为 yes/no 一个词），loss 无需自研，直接复用公司现有 swift 训练管线；0.6B 模型单卡 910B4 即可训练和部署。

**节奏**：第一版 600 query（6000 样本）定位为跑通管线 + 摸清负例规律；评测用"想要的全部排在不想的前面"的组序命中率；后续升级路径清晰（换 4B 权重、加 pairwise、挖难负例），每步互不牵连。
